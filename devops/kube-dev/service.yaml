apiVersion: v1
kind: Service
metadata:
  name: gdp-backend-dev-service
spec:
  selector:
    app: gdp-backend-dev-deploy
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP